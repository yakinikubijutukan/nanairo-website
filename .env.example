# No environment variables are required. The contact form (and its
# Resend integration) has been removed site-wide — /contact now shows
# company info only. This file is no longer needed; it could not be
# deleted from this environment (sandbox restriction), so please delete
# .env.example yourself for a fully clean tree.
