// The custom cursor has been removed site-wide in favor of the native
// system cursor (better legibility, no input lag, no distraction over
// interactive elements). This file is intentionally unused and not
// imported anywhere — kept only because this environment could not
// delete it outright. Safe to delete manually if you'd like a clean tree.
export {};
